import { FadeInProgressiveDirective } from './fade-in-progressive.directive';

describe('FadeInProgressiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInProgressiveDirective();
    expect(directive).toBeTruthy();
  });
});
