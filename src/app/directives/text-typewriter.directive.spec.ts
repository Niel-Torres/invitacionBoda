import { TextTypewriterDirective } from './text-typewriter.directive';

describe('TextTypewriterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTypewriterDirective();
    expect(directive).toBeTruthy();
  });
});
