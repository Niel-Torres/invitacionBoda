import { RotateOnLoadDirective } from './rotate-on-load.directive';

describe('RotateOnLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOnLoadDirective();
    expect(directive).toBeTruthy();
  });
});
