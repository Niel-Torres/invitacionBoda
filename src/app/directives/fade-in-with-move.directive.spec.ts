import { FadeInWithMoveDirective } from './fade-in-with-move.directive';

describe('FadeInWithMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInWithMoveDirective();
    expect(directive).toBeTruthy();
  });
});
